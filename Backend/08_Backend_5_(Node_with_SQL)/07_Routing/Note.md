## #Sigma Prime Note

### Routing

This is random layout of creating next project where we will user **REST + EJS + Mysql Server + Database**

`GET /` --> show no. of user in DB

`GET /user`   --> show users(email, id, username) ejs

`PATCH /user/:id` --> username edit


POST    /user   --> new users

DELETE   /user/:id