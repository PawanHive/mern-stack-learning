# #8: Dispatching Action (Triggering a State Change)

- The `useDispatch` hook allows you to send or dispatch an action to the redux store by giving the action as an arguments to the dispatch variable.

- the `useSelector` hooks allow you to extract data or the state from the Redux store using a selector. (returns the data)

